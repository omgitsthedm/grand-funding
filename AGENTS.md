# AI Tool Entry

Read `.ai/RULES.md`, `.ai/STATE.md`, `CLAUDE.md`, and `PROJECT_STATUS.md` before working. Current Git, Netlify, and built-artifact evidence override historical notes in generated rules.

This is a live regulated-lender site on branch `master`. Build with `npm ci && npm test`, serve only `dist/`, and run local QA against that artifact. Netlify project authority is `grandfundingllc` / `055c5942-aeaa-478a-9508-a34406994d5d`.

Git pushes do not deploy. Production deployment is manual and requires a reviewed preview, `npm run verify:netlify-target`, and clear authorization for that specific live action. Never publish the repository root.

Do not change NMLS `2466872`, AZ MLO `1048901`, rates, lending claims, disclosures, testimonials, funded-deal facts, legal content, analytics identifiers, or form destinations without approved facts. Do not submit real `/apply`, `/contact`, or programmatic form leads. Never read or expose secrets, `.env*`, client records, or submissions.

Preserve the Desert Deal Room experience, approved source content, social artwork, accessibility and reduced-motion fallbacks, and the deterministic `dist/` boundary. The 2026-07-24 reinvention is local and undeployed until explicitly released.
